from django.urls import path
from . import views

app_name = 'appointments'

urlpatterns = [
    # Appointments will primarily be accessed via patients and doctors apps
    # This app provides the models and shared utilities
]
